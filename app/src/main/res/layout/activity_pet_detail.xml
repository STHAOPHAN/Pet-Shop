<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Ảnh to của pet -->
        <ImageView
            android:id="@+id/iv_pet_image"
            android:layout_width="match_parent"
            android:layout_height="300dp"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_launcher_background" /> <!-- Placeholder image, replace with actual image -->

        <!-- ID của pet -->
        <TextView
            android:id="@+id/tv_pet_id"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="ID: 1"
            android:textSize="18sp"
            android:textColor="@color/black" />
        <!-- Tên của pet -->
        <TextView
            android:id="@+id/tv_pet_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Pet Name"
            android:textSize="24sp"
            android:textStyle="bold" />

        <!-- breed của pet -->
        <TextView
            android:id="@+id/tv_pet_breed"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Breed: Labrador"
            android:textSize="18sp"
            android:textColor="@color/black" />

        <!-- Tuổi của pet -->
        <TextView
            android:id="@+id/tv_pet_age"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Tuổi: 2"
            android:textSize="18sp"
            android:textColor="@color/black" />

        <!-- Giới tính của pet -->
        <TextView
            android:id="@+id/tv_pet_gender"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Giới tính: Male"
            android:textSize="18sp" />


        <!-- Màu sắc của pet -->
        <TextView
            android:id="@+id/tv_pet_color"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Color: Black and White"
            android:textSize="18sp" />

        <!-- Giá của pet -->
        <TextView
            android:id="@+id/tv_pet_price"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Price: $500"
            android:textSize="18sp"
            android:textColor="@android:color/holo_green_dark" />

        <!-- Nút thêm vào giỏ hàng -->
        <ImageButton
            android:id="@+id/btn_add_to_cart"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_input_add" />
    </LinearLayout>
</ScrollView>
